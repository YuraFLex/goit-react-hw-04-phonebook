import styled from 'styled-components';
import {
  layout,
  typography,
  space,
  color,
  flexbox,
  border,
  grid,
  shadow,
} from 'styled-system';

export const Box = styled('div')(
  layout,
  typography,
  space,
  color,
  flexbox,
  border,
  grid,
  shadow
);
